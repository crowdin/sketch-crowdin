{
  "name": "sketch-crowdin",
  "version": "0.1.0",
  "engines": {
    "sketch": ">=3.0"
  },
  "skpm": {
    "name": "sketch-crowdin",
    "manifest": "src/manifest.json",
    "main": "sketch-crowdin.sketchplugin"
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "dependencies": {
    "@crowdin/crowdin-api-client": "^1.0.5",
    "@skpm/buffer": "^0.1.4"
  },
  "devDependencies": {
    "@skpm/builder": "^0.4.2"
  }
}
